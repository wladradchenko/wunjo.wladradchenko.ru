#python3.11,python3.12,python3.13
# CPU wheels — no NVIDIA driver, or one older than CUDA 11.8.
# Ranges, not exact pins: which build exists depends on the interpreter the app
# runs (the runtime ships one, and it changes with it), while the upper bound is
# what this plugin was tested against. A wheel built for a newer CUDA than the
# driver refuses to start and silently falls back to the CPU, hence the split.
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple
torch>=2.5,<2.7
# torchaudio only because silero-vad imports it at module level for the file
# readers this plugin never calls; it has to match the torch above, which is why
# it comes off the same index.
torchaudio>=2.5,<2.7
numpy
scipy
librosa
soundfile
tqdm
# The separator runs its MDX model on onnxruntime; the converter does not
# need it.
onnxruntime
# The speech detector, weights inside the wheel (2.3 MB): nothing is fetched at
# render time. Wunjo v2 reached this same model through whisper_timestamped,
# which pulls it from GitHub over torch.hub on first use.
silero-vad
